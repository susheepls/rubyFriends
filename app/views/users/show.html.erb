<h1>
    <%= @user.profile_name %>
</h1>
<h2>
    <%= @user.description %>
</h2>

<%=link_to "Edit", edit_user_path(@user) %>
<%=link_to "Destroy", user_path(@user), data: {
    turbo_method: :delete,
    turbo_confirm: 'Are you sure?' 
} %>